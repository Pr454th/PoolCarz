import { RidehoverDirective } from './ridehover.directive';

describe('RidehoverDirective', () => {
  it('should create an instance', () => {
    const directive = new RidehoverDirective();
    expect(directive).toBeTruthy();
  });
});
